# The cso-classifier needs to be installed to include Ontology mining when starting the mining process.

# NOTE: does not work, but not sure if necesary
pip install nltk cso-classifier
python -m spacy download en_core_web_sm